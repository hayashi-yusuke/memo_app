<h1>メモ一覧</h1>

<div>
  <% if Current.user.avatar.attached? %>
    <%= image_tag Current.user.avatar, width: 50 %>
  <% end %>
  <%= link_to Current.user.name, edit_user_path(Current.user) %>
</div>


  <% @memos.each do |memo|%>
    <div class="memo-item">
      <%= link_to memo.content, memo_path(memo) %>
      <%= button_to "削除", memo_path(memo), method: :delete, class: "btn btn-danger btn-sm" %>
      <%= button_to "編集", edit_memo_path(memo), method: :get, class: "btn btn-success btn-sm" %>
    </div>
  <% end %>

  <%= form_with model: @memo do |f| %>
    <%= f.text_field :content, placeholder: "今日は何をメモするのかな☺️" %>
    <%= f.submit "追加" %>
  <% end %>